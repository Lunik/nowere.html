;(function(){
	
	//if (window.location.host === "lunik.xyz") {
		let selector = document.querySelectorAll(".crypto, .certificate")
		for (let div of selector) {
			div.style.display = "block"
		}
	//}
})()